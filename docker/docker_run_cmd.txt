# Upload NYU Depth v2 first:
# scripts/upload_nyu_data.sh /local/path/to/nyu_depth_v2 user@gpu-server ~/METER

# Run any training entrypoint inside the GPU Docker environment.
# DATA_DIR, OUTPUTS_DIR, CHECKPOINTS_DIR, IMAGE_NAME, and CONTAINER_NAME can be overridden.
scripts/run_server_docker.sh -- python meter_train.py \
    --data-dir /workspace/data/nyu_depth_v2 \
    --output-dir /workspace/outputs/run_001 \
    --checkpoint-dir /workspace/checkpoints

# Equivalent Docker shape:
# docker run --rm --gpus all --ipc host \
#     -v "$PWD:/workspace/METER" \
#     -v "$HOME/METER/data:/workspace/data" \
#     -v "$HOME/METER/outputs:/workspace/outputs" \
#     -v "$HOME/METER/checkpoints:/workspace/checkpoints" \
#     -w /workspace/METER \
#     meter:latest \
#     python meter_train.py --data-dir /workspace/data/nyu_depth_v2
